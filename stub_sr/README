I have all the required functionality.

I do currently see a much larger delay (a bit over a second) when the router needs to do it's own arp request and wait for the reply, however, I think this is expected behavior.